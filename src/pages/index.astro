---
import Header from '../components/Header.astro';
import SliderProyectos from '../components/SliderProyectos.astro';
import SliderEstudios from '../components/SliderEstudios.astro';
import Footer from '../components/Footer.astro';
import CtaSection from '../components/CtaSection.astro';
import { projects } from '../data/projects';
import { studios } from '../data/studios';

const latestYear = Math.max(...projects.map((project) => project.year));
const highlightedProjects = projects.filter((project) => project.year === latestYear);
---


<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" href="https://www.porcelanosa.com/PIcon.png" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<title>10 de 10 — Home</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet" />
		<style>
			:root{--c-white:#ffffff;--c-black:#000000;--c-grey-84:#d5d5d5;--c-grey-46:#767676;--c-grey-34:#575757;--c-grey-16:#2b2c28;--c-grey-100:#939393}
			*{box-sizing:border-box}
			html,body{margin:0;padding:0;overflow-x:hidden}
			body{font-family:Lato,system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Cantarell,Noto Sans,"Helvetica Neue",Arial,"Apple Color Emoji","Segoe UI Emoji";color:#000;background:#fff}
			.max{max-width:1728px;margin:0 auto}
			.container{max-width:1600px;margin:0 auto}
			.px16{padding-left:16px;padding-right:16px}
			.px20{padding-left:20px;padding-right:20px}
			.pt150{padding-top:150px}
			.py12{padding-top:48px;padding-bottom:48px}
			.py20{padding-top:80px;padding-bottom:80px}
			.py16{padding-top:64px;padding-bottom:64px}
			.py5{padding-top:20px;padding-bottom:20px}
			.py0{padding-top:0;padding-bottom:0}
			.mt24{margin-top:24px}
			.mt48{margin-top:48px}
			.mb0{margin-bottom:0}
			.center{text-align:center}
			.uppercase{text-transform:uppercase}
			.grid{display:grid;gap:24px}
			.grid-4{grid-template-columns:repeat(4,minmax(0,1fr))}
			.grid-5{grid-template-columns:repeat(5,minmax(0,1fr))}
			.row{display:flex;gap:24px}
			.between{display:flex;justify-content:space-between;align-items:center}
			header .logo{font-family:"Porcelanosa",serif;font-weight:400;font-size:32px;line-height:64px;letter-spacing:.02em}
			.hero{background:#000;height:100vh;position:relative;display:flex;align-items:center;justify-content:center;overflow:hidden}
			.hero-video{position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover;z-index:0}
			.hero .autoplay{color:#fff;font-size:14px;position:relative;z-index:2}
			.hero .cta-arrow{position:absolute;right:32px;bottom:32px;height:50px;width:50px;border:2px solid #fff;display:grid;place-items:center;z-index:2;transform:rotate(90deg)}
			.section-title{font-weight:700;color:var(--c-grey-16);font-size:30px}
			.section-sub{color:var(--c-grey-46);font-size:18px;line-height:1.5}
			.underline{position:relative;display:inline-block}
			.underline:after{content:"";position:absolute;left:0;right:0;bottom:-1px;height:1px;background:#000}
			.card figure{aspect-ratio:16/11;background:#eee;overflow:hidden;margin:0}
			.card figure img{width:100%;height:100%;object-fit:cover;display:block}
			.card .meta{display:flex;gap:12px;color:var(--c-grey-46);font-size:14px;margin-top:4px;margin-bottom:6px}
			.card .title{font-weight:700;color:#000;text-transform:uppercase;font-size:18px}
			.card-link{display:block;height:100%;text-decoration:none;color:inherit;cursor:pointer}
			.studios .studio{display:flex;flex-direction:column;gap:8px}
			.studio .thumb{aspect-ratio:412/400;background:#eee}
			.cta-block{display:flex;gap:48px;align-items:flex-end;justify-content:center}
			.cta-block .image{width:600px;max-width:100%;aspect-ratio:1/1;background-size:cover;background-position:center}
			.cta-block .copy{width:600px;max-width:100%}
			.link-stroke{height:50px;display:grid;place-items:center;padding:0 22px;border:2px solid #000}
			.link-more{font-size:16px;padding:8px 0;display:inline-block}
			footer.site-footer{border-top:1px solid var(--c-grey-84)}
			.footer-top{border-bottom:1px solid var(--c-grey-84)}
			.footer-menu{display:flex;gap:16px}
			.footer-menu a{font-weight:700;font-size:16px;line-height:26px;color:#767676;text-decoration:none;text-transform:uppercase}
			.social{display:flex;gap:12px}
			.social .dot{height:32px;width:32px;border-radius:16px;background:#000;display:grid;place-items:center}
			.copyright{color:var(--c-grey-100);font-size:12.8px}
			.brandmark{height:140px;width:190px;margin:0 auto}
			/* Simple responsive adjustments */
			@media (max-width: 1200px){
				.grid-4{grid-template-columns:repeat(2,minmax(0,1fr))}
				.cta-block{flex-direction:column;align-items:center}
			}
			@media (max-width: 680px){
				.grid-4{grid-template-columns:1fr}
				.hero{height:420px}
			}
		</style>
			<link rel="stylesheet" href="/assets/css/brand.css" />
	</head>
	<body style="position: relative;">
		<Header />

		<div class="hero">
			<video class="hero-video" autoplay muted loop playsinline>
				<source src="/videos/home-video.mp4" type="video/mp4">
			</video>
			<a href="#content" class="cta-arrow" aria-label="Ir al contenido">
				<img src="assets/img/c582bf20c9c9867b584a5c44488cc4a93ecd5090.svg" alt="" height="20" width="12" />
			</a>
		</div>

		<main>
			<section id="content" class="px16 py20">
				<div class="container center">
					<h2 class="section-title mb0">10 de 10: Diseño que conecta con la materia</h2>
					<p class="section-sub mt24">Una celebración del diseño contemporáneo donde la creatividad, la sostenibilidad y la conexión con la naturaleza se transforman en experiencias habitables.  Cada edición selecciona 10 despachos que reinterpretan los materiales y las texturas de la marca a través de espacios únicos en México.</p>
					<div class="mt24" style="color:var(--c-grey-46);font-weight:700;">SABER MÁS SOBRE 10 DE 10</div>
				</div>
			</section>

				<SliderProyectos projects={highlightedProjects} />

			<div class="mt48">
				<SliderEstudios studios={studios} />
			</div>

		</main>

		<CtaSection/>
		<Footer/>

		<script>
			// Smooth scroll for CTA arrow
			document.querySelector('.cta-arrow')?.addEventListener('click', function(e) {
				e.preventDefault();
				const target = document.getElementById('content');
				if (target) {
					target.scrollIntoView({
						behavior: 'smooth',
						block: 'start'
					});
				}
			});
		</script>
	</body>
</html>
